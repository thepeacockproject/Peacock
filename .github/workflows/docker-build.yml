name: Build and Push Peacock (Multi-Cloud)

on:
  workflow_dispatch: # Manual trigger
  schedule:
    - cron: '0 0 * * *' # Runs daily at midnight UTC

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Convert repository owner to lowercase
        id: lowercase
        run: |
          echo "owner_lowercase=${OWNER,,}" >> $GITHUB_OUTPUT
        env:
          OWNER: ${{ github.repository_owner }}

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Validate tags are lowercase
        run: |
          echo "Validating Docker tags..."
          DOCKER_TAG="${{ secrets.DOCKERHUB_USERNAME }}/peacock:latest"
          GHCR_TAG="ghcr.io/${{ steps.lowercase.outputs.owner_lowercase }}/peacock:latest"
          
          echo "Docker Hub tag: ${DOCKER_TAG}"
          echo "GHCR tag: ${GHCR_TAG}"
          
          # Check if Docker Hub username contains uppercase letters
          if [[ "${{ secrets.DOCKERHUB_USERNAME }}" =~ [A-Z] ]]; then
            echo "❌ ERROR: Docker Hub username contains uppercase letters!"
            echo "Please update your DOCKERHUB_USERNAME secret to be lowercase."
            exit 1
          else
            echo "✅ Docker Hub username is lowercase: ${{ secrets.DOCKERHUB_USERNAME }}"
          fi
          
          # Check if GHCR tag is lowercase
          if [[ "${GHCR_TAG}" =~ [A-Z] ]]; then
            echo "❌ ERROR: GHCR tag contains uppercase: ${GHCR_TAG}"
            exit 1
          else
            echo "✅ GHCR tag is lowercase: ${GHCR_TAG}"
          fi

      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/peacock:latest
            ghcr.io/${{ steps.lowercase.outputs.owner_lowercase }}/peacock:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
