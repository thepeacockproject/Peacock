name: Build and Push Peacock (Multi-Cloud)

on:
  workflow_dispatch: # Manual trigger
  schedule:
    - cron: '0 0 * * *' # Runs daily at midnight UTC

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Convert repository owner to lowercase
        id: lowercase
        run: |
          echo "owner_lowercase=${OWNER,,}" >> $GITHUB_OUTPUT
        env:
          OWNER: ${{ github.repository_owner }}

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Validate tags are lowercase
        run: |
          echo "Validating Docker tags..."
          echo "Docker Hub tag: ${{ secrets.DOCKERHUB_USERNAME }}/peacock:latest"
          echo "GHCR tag: ghcr.io/${{ steps.lowercase.outputs.owner_lowercase }}/peacock:latest"
          
          # Check if Docker Hub username contains uppercase letters
          if [[ "${{ secrets.DOCKERHUB_USERNAME }}" =~ [A-Z] ]]; then
            echo "⚠️ WARNING: Docker Hub username contains uppercase letters!"
            echo "This may cause build failures. Please use lowercase in your secret."
          else
            echo "✅ Docker Hub username is lowercase: ${{ secrets.DOCKERHUB_USERNAME }}"
          fi
          
          # Check if GHCR tag is lowercase
          if [[ "${{ steps.lowercase.outputs.owner_lowercase }}" =~ [A-Z] ]]; then
            echo "❌ ERROR: GHCR tag still contains uppercase!"
            exit 1
          else
            echo "✅ GHCR tag is lowercase: ghcr.io/${{ steps.lowercase.outputs.owner_lowercase }}/peacock:latest"
          fi

      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/peacock:latest
            ghcr.io/${{ steps.lowercase.outputs.owner_lowercase }}/peacock:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
